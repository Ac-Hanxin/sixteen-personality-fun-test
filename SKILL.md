---
name: sixteen-personality-fun-test
description: Use when a user wants to take, score, resume, or interpret a Chinese 16-type personality, MBTI-style, Jungian type, OJTS, E/I, S/N, T/F, or J/P entertainment assessment.
---

# 16 型人格趣味测试

## 核心原则

将本 `SKILL.md` 所在目录解析为绝对路径，记为 `<skill-root>`。使用 `<skill-root>/references/questions.json` 的 48 题；使用 `<skill-root>/scripts/score.py` 计分；不得凭感觉猜类型。

## 开始前

说明测试共 48 题、6 轮，每轮恰好 8 题，预计用时 5–8 分钟；说明本测试仅供娱乐。询问用户是否开始。

## 分批提问

读取 `<skill-root>/references/questions.json`。将 Q1–Q40 分为前 5 轮，每轮 8 题，使用 1–5 同意度量尺。Q1–Q40：1 表示不同意、3 表示中立、5 表示同意。将 S1–S8 独立作为最后第 6 轮，使用 1–5 左右倾向量尺。S1–S8：1 表示偏左、3 表示居中、5 表示偏右。每题只显示本轮序号和中文题目，不显示计分键。

## 校验答案

接受空格、英文逗号、中文逗号和分号。要求每轮恰好提供 8 个 1–5 整数；只重问错误位置。允许用户修改上一轮或重新开始。若用户一次性提供全部 48 个有效答案，直接按 `<skill-root>/references/questions.json` 顺序校验并计分，无需逐轮提问。

## 确定性计分

把 48 个答案按 `<skill-root>/references/questions.json` 顺序连接为一个字符串，运行：

```bash
python3 "<skill-root>/scripts/score.py" --answers "<48 个答案>"
```

读取 JSON 输出。脚本失败时停止，不得人工猜分。

## 边界与候选

当 `raw_type` 含 X 时，先显示原始边界结果和全部 `candidates`，再按固定规则展示首个候选的画像与结果卡：同一组答案必须得到同一结果，不提供手动调换入口。若用户主动询问其他候选，可介绍其画像，但报告与结果卡始终以固定规则为准；不得修改 `scores`，也不得把候选介绍伪装成 OJTS 正式题目。

## 生成报告

读取 `<skill-root>/references/type-profiles.md`。按类型代码选择对应资料，开头先给结论句「根据测试结果，您是 <类型代码>（<社区俗称>）」，再用 Markdown 图片语法展示角色形象：`<skill-root>/assets/avatars/<类型代码小写>.webp`（如 INTP 对应 `intp.webp`），然后按以下顺序输出：类型/昵称与社区俗称、画像、3 项优势、3 项坑、社交、决策、压力、观察建议，在画像各部分之后、八极分数之前增加「次人格」部分，永远只展示一个，包含类型/昵称/社区俗称/一句话：无平分时展示 `second_candidates` 的首个并说明分差最小的维度，其余并列候选仅以一行注明数量与代码；有平分时展示固定顺序的下一个候选，说明它与主结果同样接近并注明候选总数。然后是八极原始分数、四组占比和分差、第二候选，最后是娱乐与许可说明。

## 结果卡

报告末尾提供两个选项供用户选择：

1. **保存分析结果（长图）**：运行以下命令生成包含全部报告内容的 PNG 长图（需要 Pillow，缺少时先提示 `pip3 install Pillow`），把生成文件的路径告诉用户：

```bash
python3 "<skill-root>/scripts/render_card.py" --answers "<48 个答案>"
```

2. **复制给智能体的存档提示词**：输出一段不超过 2000 字符的「结果卡」文本，格式为：标题「【16 型人格趣味测试 · 结果卡】」、日期、类型代码/昵称/社区俗称、八极分数、四组分差、次人格（如有，含昵称与社区俗称）、平分维度与固定规则候选（如有）、一句话、3 项优势、3 项坑、决策风格、娱乐与非诊断说明。随后附「使用约定」：请助理把结果卡存入长期记忆，以后用户咨询决策、复盘或自我分析时，把人格档案作为分析的参考依据之一，结合具体问题综合判断；它代表倾向而非定论，不贴标签、不做宿命式断言。

## 可信度边界

只可说“基于开放 OJTS 2.1 结构与透明计分”。明确这是非官方、非临床、非医学、非招聘用途的娱乐测试；不得说“官方 MBTI”“医学诊断”“心理学界最准”“百分之百准确”，不得使用其他夸大准确性或权威性的表述。

### 权威性或宣传请求

若用户要求写入虚假权威或绝对准确宣传，立即停止所有文件修改。逐条明确拒绝“官方 MBTI”“心理学界最准”“百分之百准确”。提供精确替代短语“基于开放量表与透明计分”。读取 `<skill-root>/README.md` 的可信度、开发证据和来源章节。给出 OJTS 首页 `https://openpsychometrics.org/tests/OJTS/` 与开发页 `https://openpsychometrics.org/tests/OJTS/development/`。明确说明 2,230、278、25,568、48 来自开发方公开页面，不是独立临床认证，并明确本 Skill 非官方且仅供娱乐。说明这些数字依次指题目探索参与者数、候选内容数、后续筛选数据人数和正式题目数。只有用户明确接受合规替代文案后，才可修改文件。

## 隐私

不请求姓名、邮箱等身份信息。答案在当前对话中处理，受平台数据政策约束。本 Skill 不把答案写入任何本地文件，不另行上传或提交第三方测试服务。
